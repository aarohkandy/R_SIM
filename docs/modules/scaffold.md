# Scaffold Notes

- Keep Phase 0 limited to repo structure, config loading, deterministic seeding,
  placeholder logging, task commands, and test infrastructure.
- Do not implement later physics phases during scaffold work.
- Every placeholder belongs in both `ASSUMPTIONS.md` and `PROGRESS.md`.

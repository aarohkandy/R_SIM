# Renode

Phase 12 will add Renode `.resc` scripts, `.repl` platform files, and co-simulation
glue for the ESP32 and Teensy firmware.

Renode is not required for Phase 0. Installation notes live in `docs/tooling.md`.
